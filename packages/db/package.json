{
  "name": "@auditsmart/db",
  "version": "1.0.0",
  "private": true,
  "description": "Prisma client + repository pattern — single source of truth for all DB access",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build":     "tsc --build",
    "generate":  "prisma generate",
    "migrate":   "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma":     "^5.22.0",
    "typescript": "^5.4.5"
  }
}
